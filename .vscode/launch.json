{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "debug",
      "request": "launch",
      "command": "yarn run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
